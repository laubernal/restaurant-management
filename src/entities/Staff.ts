export class Staff {
  salary: number;
  name: string;
  occupation: string;

  constructor(salary: number, name: string, occupation: string) {
    this.salary = salary;
    this.name = name;
    this.occupation = occupation;
  }
}
